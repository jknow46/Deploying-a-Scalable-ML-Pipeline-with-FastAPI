name: get_data

conda_env: conda.yml

entry_points:
  main:
    parameters:
      sample:
        type: string
      artifact_name:
        type: string
      artifact_type:
        type: string
      artifact_description:
        type: string
    command: >
      python run.py
      {sample}
      {artifact_name}
      {artifact_type}
      {artifact_description}