name: test_regression_model

conda_env: conda.yml

entry_points:
  main:
    parameters:
      model_export:
        type: string
      test_data:
        type: string
    command: >
      python run.py
      --model_export {model_export}
      --test_data {test_data}