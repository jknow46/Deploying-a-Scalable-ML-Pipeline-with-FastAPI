name: nyc_airbnb

conda_env: conda.yml

entry_points:
  main:
    parameters:
      steps:
        type: string
        default: "all"
      hydra_options:
        type: string
        default: ""
    command: >
      python main.py main.steps='{steps}' {hydra_options}