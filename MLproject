name: nyc_airbnb

conda_env: conda.yml

entry_points:
  main:
    parameters:
      steps:
        type: string
        default: "all"
      sample:
        type: string
        default: "sample.csv"
    command: >
      python main.py main.steps='{steps}' etl.sample='{sample}'